{% extends 'base.html.twig' %}

{% block title %}Liste des albums !{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-3">Albums</h1>

    <div class="row">
        {% for album in albums %}
            <div class="col-md-4">
                <div class="card bg-light mb-3">
                    <div class="card-header text-center">
                        {{ album.title }}
                        <small>par {{ album.creationUser }}</small>
                    </div>
                    <div class="card-body">
                        <h4 class="card-title">{{ album.albumDate|date('d-m-Y H:i:s') }}</h4>
                        <p class="card-text">
                            {{ album.content }}
                        </p>
                        <a href="#" class="btn btn-primary">En voir plus !</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
